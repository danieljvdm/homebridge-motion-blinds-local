/**
 * This is the name of the platform that users will use to register the plugin in the Homebridge config.json
 */
export const PLATFORM_NAME = 'MotionBlindsLocal';

/**
 * This must match the name of your plugin as defined the package.json `name` property
 */
export const PLUGIN_NAME = 'homebridge-motion-blinds-local';

/**
 * Motion Blinds UDP port
 */
export const UDP_PORT = 32100;

/**
 * Multicast address for Motion Blinds discovery
 */
export const MULTICAST_ADDRESS = '238.0.0.18';
